{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The orb only renders state; it needs no privileged APIs.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
