# --- 音声入出力 (step 2) ---
sounddevice>=0.4.6
numpy>=1.26

# --- 音声認識 (step 3) ---
faster-whisper>=1.0.3

# --- 応答生成 (step 4) ---
anthropic>=1.4,<2
python-dotenv>=1.0.1

# --- 音声合成 (step 5) ---
requests>=2.32
